pub fn stars(n: u32) -> String {
    let base: i32 = 2;
    let exponent: u32 = 10;
    let result = base.pow(exponent); // 1024
    println!("{} raised to {} is {}", base, exponent, result);
    // let number_repeat = 2_i32.pow(n as );
    // "*".repeat(number_repeat)
    todo!()
}
